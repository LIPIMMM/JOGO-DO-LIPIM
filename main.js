// Seleção de elementos HTML

const Mario=document.querySelector('.Mario')
const pipe=document.querySelector('pipe')
const sta//rtButton=document.querySelector('starButton')
const restartButton=document.querySelector('restartButton')
const gameOver=document.querySelector('gameOver')

// Definição de Audios

const audioStart=new Audio ('sound/audio_theme.mp3')
const audioGameOver=new Audio ('sound/audio_gameover.mp3')
const audioMario=new Audio ('sound/MARIO_soung_audio_theme.mp3')

// Variaveis de Controle do jogo

let gemeInterval=null

 //funçẽs para iniciar o jogo

const startGame=()=>{}
pipe.classList.add('pipe-animation')

// oculta o botão de "iniciar" para evitar cliques acidentais

startBotton.style.display = 'none'

// oculta a tela de gameOver (do jogo anterior)

gameOver.style.display = 'none'

// iniciar a reprodução da musica tema

audioStart.correntTime = 0

// iniciar a reprodução da musica tema

audioStart.play()

// verificar se o intervalo do jogo já não está rodando

if(!gameInterval){
    // criar um intervalo que executa a função gameloop a cada 10 milisegundos
    // isso atualiza a detecção de colisão continuamente

gameInterval = s






}

